# Copy to .env to override the default target environments.
# Both are optional — the suite defaults to the public demo sites.

# UI_BASE_URL=https://www.saucedemo.com
# API_BASE_URL=https://restful-booker.herokuapp.com
